<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="gismeteo_gui.MainPage">

    <Grid RowDefinitions="Auto, *" ColumnDefinitions="*">
        <ScrollView x:Name="scroll1" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
            <Grid x:Name="menu" Padding="10" HorizontalOptions="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
            </Grid>
        </ScrollView>
        <ScrollView Orientation="Vertical" VerticalOptions="FillAndExpand" x:Name="scroll2" Grid.Row="1">
            <Grid x:Name="table" Padding="10">
            
            </Grid>
        </ScrollView>
    </Grid>
</ContentPage>

